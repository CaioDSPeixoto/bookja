// Root layout exists only as required by Next.js.
// The real layout is in [locale]/layout.tsx.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
